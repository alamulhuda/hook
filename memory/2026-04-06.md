# 2026-04-06 - Inertia Migration Session

## What Happened

### Fixed Multiple Build Issues
1. Removed conflicting `vite.config.js` that was overriding `vite.config.ts`
2. Fixed Vue components with `VariantProps` and `HTMLAttributes` issues
3. Converted `dropdown-menu` and `sheet` components to barrel file directory structure
4. Created missing `card-title.vue` component
5. Fixed `AppLayout.vue` duplicate `defineProps` blocks
6. Added CSS import to `app.ts`

### Build Now Works
- `npm run inertia:build` produces proper bundles:
  - `app-*.js` at ~215KB (contains Vue + Inertia + all code)
  - `app-*.css` at ~114KB (contains Tailwind CSS)

### Still Has Blank Page Issue
- The `@vite` directive in blade still points to Vite dev server (`127.0.0.1:5173`)
- This happens because `APP_DEBUG=true` in `.env`
- Build assets are correct in `public/build/`

## Solution to Test Next Time
Set `APP_DEBUG=false` in `.env` and clear config:
```bash
docker exec arabica-app php artisan config:clear
```
Then refresh `/app/dashboard`

## Files Modified
- `vite.config.ts` - Reconfigured with laravel plugin
- `resources/js/app.ts` - Fixed resolve function, added CSS import
- `resources/views/app.blade.php` - Fixed vite paths
- `resources/js/components/ui/*.vue` - Multiple fixes
- `bootstrap/app.php` - Added HandleInertiaRequests middleware
